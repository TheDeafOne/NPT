import Section from "../Section"

const SkillsSection = (ref: any) => {
    return (
        <Section ref={ref}>
            <div id="skills-section" className="bg-black text-white">
                skills
            </div>
        </Section>
    )
}

export default SkillsSection