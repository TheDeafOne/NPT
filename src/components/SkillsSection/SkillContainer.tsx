import { useEffect } from "react";
const SkillContainer = ({title}: any) => {
    return (
        <div className="bg-black rounded-full h-20 w-20 flex justify-center items-center">
            {title}
        </div>
    )
}

export default SkillContainer;